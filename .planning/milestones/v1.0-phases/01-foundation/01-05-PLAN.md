---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - TorchKeeper.Tests/Services/CharacterFileServiceTests.cs
autonomous: true
gap_closure: true
requirements:
  - FILE-02
  - FILE-03

must_haves:
  truths:
    - "dotnet test TorchKeeper.Tests/ exits 0 with 9 tests passing (7 import + 2 file service)"
    - "CharacterFileServiceTests compiles without CS7036 errors"
    - "RoundTrip_SaveLoad_NoDataLoss passes, confirming MapToDto/MapFromDto/LoadFromStreamAsync round-trip"
    - "Save_ContainsVersionField passes, confirming Version=1 is serialized to .sdchar JSON"
  artifacts:
    - path: "TorchKeeper.Tests/Services/CharacterFileServiceTests.cs"
      provides: "Compilable test fixture for CharacterFileService"
      contains: "NullFileSaver"
  key_links:
    - from: "CharacterFileServiceTests.cs"
      to: "CharacterFileService.cs"
      via: "new CharacterFileService(new NullFileSaver())"
      pattern: "new CharacterFileService\\(new NullFileSaver"
---

<objective>
Fix the CS7036 compile error in CharacterFileServiceTests.cs so that the 2 file-service tests compile and pass, unblocking FILE-02 and FILE-03 verification.

Purpose: The only remaining Phase 1 gap is a single line in the test fixture. CharacterFileService now requires IFileSaver via constructor injection, but the test still uses the old no-arg `new()` syntax. The service logic is correct; the test setup is not.

Output: A compilable test file with a NullFileSaver stub, 9 tests passing total.
</objective>

<execution_context>
@/Users/daren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CharacterFileServiceTests — replace no-arg new() with NullFileSaver</name>
  <files>TorchKeeper.Tests/Services/CharacterFileServiceTests.cs</files>
  <action>
The only constructor on CharacterFileService is `public CharacterFileService(IFileSaver fileSaver)`. The test fixture currently uses `private readonly CharacterFileService _service = new();` which is a CS7036 compile error.

Fix using Option A (no production code changes):

1. Add a private `NullFileSaver` nested class inside `CharacterFileServiceTests` that implements `CommunityToolkit.Maui.Storage.IFileSaver`. Provide a minimal implementation — `SaveAsync` can return a completed `FileSaverResult` with a dummy path and no exception, or simply throw `NotImplementedException` since neither test calls SaveAsync.

   ```csharp
   private sealed class NullFileSaver : IFileSaver
   {
       public Task<FileSaverResult> SaveAsync(string initialPath, string fileName, Stream stream, CancellationToken cancellationToken = default)
           => Task.FromResult(new FileSaverResult("/dev/null", null));

       public Task<FileSaverResult> SaveAsync(string fileName, Stream stream, CancellationToken cancellationToken = default)
           => Task.FromResult(new FileSaverResult("/dev/null", null));
   }
   ```

   Note: Check the exact `IFileSaver` interface members from CommunityToolkit.Maui 9.x — it may have one or two `SaveAsync` overloads. Implement all required members. If `FileSaverResult` requires different constructor arguments, adjust accordingly (it typically takes a `string filePath` and an `Exception? exception`).

2. Replace line 14:
   - Old: `private readonly CharacterFileService _service = new();`
   - New: `private readonly CharacterFileService _service = new(new NullFileSaver());`

3. Add any necessary `using` statements for `CommunityToolkit.Maui.Storage` and `System.Threading` if not already present.

4. Do NOT modify any test methods — the two test methods themselves are correct and complete.

5. Do NOT modify CharacterFileService.cs — no production code changes needed.
  </action>
  <verify>
    <automated>cd /Users/daren/projects/sd-character-sheet && dotnet test TorchKeeper.Tests/ --no-build 2>&1 | tail -20 || dotnet test TorchKeeper.Tests/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    - dotnet test TorchKeeper.Tests/ exits with code 0
    - Output shows "9 passed, 0 failed" (7 import tests + 2 file service tests)
    - No CS7036 or any other compile error in the output
    - RoundTrip_SaveLoad_NoDataLoss: PASSED
    - Save_ContainsVersionField: PASSED
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm all 9 tests pass:

```
dotnet test TorchKeeper.Tests/
```

Expected output contains:
- `Passed!  - Failed: 0, Passed: 9, Skipped: 0`
- No compilation errors
- Both `RoundTrip_SaveLoad_NoDataLoss` and `Save_ContainsVersionField` listed as passed

If the NullFileSaver interface members are wrong (interface changed between CommunityToolkit.Maui versions), check the actual interface with:
```
grep -r "IFileSaver" TorchKeeper/ --include="*.cs" -l
```
Then inspect the interface source or package to get the correct method signatures.
</verification>

<success_criteria>
- CharacterFileServiceTests.cs compiles without errors
- All 9 tests pass (dotnet test exits 0)
- FILE-02 (load .sdchar) and FILE-03 (save .sdchar) test coverage is verified
- Phase 1 success criteria #2 ("character can be saved and loaded with no data loss") is now fully verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` following the standard summary template.
</output>
